# yt-sub-ext 
https://github.com/Kostyaov/yt_sub_ext/releases
# YouTube Ukrainian TTS

Розширення для Chrome, яке автоматично озвучує українські субтитри на YouTube використовуючи високоякісну технологію EdgeTTS від Microsoft.


## Зміст

- [Можливості](#можливості)
- [Встановлення](#встановлення)
  - [Встановлення Python-сервера](#встановлення-python-сервера)
    - [Простий спосіб (для новачків)](#простий-спосіб-для-новачків)
    - [Професійний спосіб (з віртуальним середовищем)](#професійний-спосіб-з-віртуальним-середовищем)
  - [Встановлення розширення Chrome](#встановлення-розширення-chrome)
- [Використання](#використання)
- [Статуси іконки](#статуси-іконки)
- [Структура проєкту](#структура-проєкту)
- [Розробка](#розробка)
- [Поширені проблеми](#поширені-проблеми)
- [Ліцензія](#ліцензія)

## Можливості

- 🔊 Автоматичне виявлення та озвучення українських субтитрів на YouTube
- 🗣️ Два голоси на вибір — Поліна (жіночий) та Остап (чоловічий)
- ⏩ Регулювання швидкості озвучення від 100% до 200%
- 💻 Простий і зручний інтерфейс у стилі YouTube
- 🔄 Можливість швидко вмикати та вимикати озвучення
- 🔔 Індикація статусу роботи через іконку розширення

## Встановлення

### Встановлення Python-сервера

#### Передумови

- Python 3.8 або новіше: [завантажити з python.org](https://www.python.org/downloads/)
- При встановленні Python переконайтеся, що обрали опцію "Add Python to PATH"

#### Простий спосіб (для новачків)

Цей спосіб підходить, якщо ви не працюєте з іншими Python-проєктами.

##### Windows

1. **Завантажте проєкт**:
   - Скачайте ZIP-архів цього репозиторію і розпакуйте його
   - Або клонуйте репозиторій: `git clone https://github.com/Kostyaov/yt_sub_ext/releases`

2. **Встановіть залежності**:
   - Відкрийте командний рядок від імені адміністратора
   - Перейдіть до папки проєкту: `cd шлях\до\yt_sub_ext`
   - Виконайте: `pip install fastapi==0.104.1 pydantic==2.4.2 uvicorn==0.24.0 edge-tts==6.1.9`

3. **Запустіть сервер**:
   - Двічі клацніть на файлі `start_server.bat` у кореневій папці проєкту
   - Або виконайте: `python edge-tts-server\server.py`

##### macOS / Linux

1. **Завантажте проєкт**:
   - Скачайте ZIP-архів цього репозиторію і розпакуйте його
   - Або клонуйте репозиторій: `git clone https://github.com/Kostyaov/yt_sub_ext/releases`

2. **Встановіть залежності**:
   - Відкрийте Terminal
   - Перейдіть до папки проєкту: `cd шлях/до/yt_sub_ext`
   - Виконайте: `pip3 install fastapi==0.104.1 pydantic==2.4.2 uvicorn==0.24.0 edge-tts==6.1.9`

3. **Запустіть сервер**:
   - В Terminal надайте права на виконання скрипту: `chmod +x start_server.command`
   - Двічі клацніть на файлі `start_server.command` у кореневій папці проєкту
   - Або виконайте: `python3 edge-tts-server/server.py`

#### Професійний спосіб (з віртуальним середовищем)

Цей спосіб рекомендований для уникнення конфліктів з іншими Python-проєктами.

##### Windows

1. **Завантажте проєкт**:
   - Скачайте ZIP-архів цього репозиторію і розпакуйте його
   - Або клонуйте репозиторій: `git clone https://github.com/Kostyaov/yt_sub_ext/releases`

2. **Створіть віртуальне середовище**:
   - Відкрийте командний рядок
   - Перейдіть до папки проєкту: `cd шлях\до\yt_sub_ext`
   - Створіть віртуальне середовище: `python -m venv venv`
   - Активуйте віртуальне середовище: `venv\Scripts\activate`
   - Ви повинні побачити `(venv)` на початку рядка

3. **Встановіть залежності**:
   - Переконайтеся, що віртуальне середовище активоване (`(venv)` на початку рядка)
   - Виконайте: `pip install fastapi==0.104.1 pydantic==2.4.2 uvicorn==0.24.0 edge-tts==6.1.9`
   - Або: `pip install -r edge-tts-server\requirements.txt`

4. **Модифікуйте скрипт запуску** (необов'язково, але рекомендовано):
   - Відкрийте файл `start_server.bat` в текстовому редакторі
   - Додайте наступні рядки перед запуском сервера:
   ```batch
   call venv\Scripts\activate
   ```

5. **Запустіть сервер**:
   - Двічі клацніть на модифікованому файлі `start_server.bat`
   - Або виконайте наступні команди в командному рядку:
   ```batch
   venv\Scripts\activate
   python edge-tts-server\server.py
   ```

##### macOS / Linux

1. **Завантажте проєкт**:
   - Скачайте ZIP-архів цього репозиторію і розпакуйте його
   - Або клонуйте репозиторій: `git clone https://github.com/Kostyaov/yt_sub_ext/releases`

2. **Створіть віртуальне середовище**:
   - Відкрийте Terminal
   - Перейдіть до папки проєкту: `cd шлях/до/yt_sub_ext`
   - Створіть віртуальне середовище: `python3 -m venv venv`
   - Активуйте віртуальне середовище: `source venv/bin/activate`
   - Ви повинні побачити `(venv)` на початку рядка

3. **Встановіть залежності**:
   - Переконайтеся, що віртуальне середовище активоване (`(venv)` на початку рядка)
   - Виконайте: `pip install fastapi==0.104.1 pydantic==2.4.2 uvicorn==0.24.0 edge-tts==6.1.9`
   - Або: `pip install -r edge-tts-server/requirements.txt`

4. **Модифікуйте скрипт запуску** (необов'язково, але рекомендовано):
   - Відкрийте файл `start_server.command` в текстовому редакторі
   - Додайте наступні рядки перед запуском сервера:
   ```bash
   source venv/bin/activate
   ```

5. **Запустіть сервер**:
   - В Terminal надайте права на виконання скрипту: `chmod +x start_server.command`
   - Двічі клацніть на модифікованому файлі `start_server.command`
   - Або виконайте наступні команди в Terminal:
   ```bash
   source venv/bin/activate
   python3 edge-tts-server/server.py
   ```

### Встановлення розширення Chrome

1. **Відкрийте сторінку розширень Chrome**:
   - Введіть `chrome://extensions/` в адресному рядку Chrome
   - Або відкрийте меню Chrome -> Додаткові інструменти -> Розширення

2. **Увімкніть режим розробника**:
   - Увімкніть перемикач "Режим розробника" у правому верхньому куті

3. **Завантажте розширення**:
   - Натисніть "Завантажити розпаковане"
   - Виберіть папку з розширенням (папка `chrome-extension` у проєкті)
   - Розширення повинно з'явитися у списку

## Використання

1. **Запустіть сервер**:
   - Запустіть Python-сервер за допомогою скрипту, як описано вище
   - Сервер має бути активним під час використання розширення

2. **Відкрийте відео на YouTube**:
   - Перейдіть на YouTube і відкрийте будь-яке відео
   - Увімкніть українські субтитри на відео (автоматичні або ручні)

3. **Налаштуйте розширення**:
   - Натисніть на іконку розширення в панелі інструментів Chrome
   - Увімкніть або вимкніть озвучування за допомогою перемикача
   - Виберіть голос (Поліна або Остап)
   - Встановіть бажану швидкість озвучування
   - Натисніть "Зберегти налаштування"

4. **Насолоджуйтеся озвученими субтитрами**:
   - Розширення автоматично виявлятиме та озвучуватиме українські субтитри
   - Регулюйте гучність звуку в YouTube (не впливає на озвучування субтитрів)

## Статуси іконки

Розширення відображає різні стани через іконку в панелі інструментів:

- **Активно** - розширення працює, сервер запущено
- **Вимкнено** - розширення вимкнено в налаштуваннях, але сервер працює
- **Помилка** - сервер не запущено або недоступний

## Структура проєкту

```
youtube-ukrainian-tts/
├── chrome-extension/         # Файли розширення Chrome
│   ├── background.js         # Фоновий скрипт розширення
│   ├── content.js            # Контентний скрипт для інтеграції з YouTube
│   ├── manifest.json         # Маніфест розширення
│   ├── popup.html            # HTML інтерфейсу налаштувань
│   ├── popup.js              # Скрипт інтерфейсу налаштувань
│   └── icons/                # Іконки розширення
│       ├── icon16.png        # Звичайна іконка (16x16)
│       ├── icon16_disabled.png # Іконка вимкненого стану
│       └── icon16_error.png  # Іконка помилки сервера
│       # ... та інші розміри (32, 48, 128)
├── edge-tts-server/          # Серверна частина
│   ├── server.py             # Python-сервер для EdgeTTS
│   └── requirements.txt      # Залежності Python
├── venv/                     # Віртуальне середовище Python (створюється автоматично)
├── start_server.bat          # Скрипт запуску сервера для Windows
├── start_server.command      # Скрипт запуску сервера для macOS
└── README.md                 # Цей файл
```

## Розробка

Для розробників, які хочуть модифікувати або вдосконалювати це розширення:

1. **Налаштування середовища розробки**:
   - Рекомендується використовувати віртуальне середовище, як описано вище
   - Використовуйте VS Code або інше IDE для зручного редагування коду

2. **Модифікація інтерфейсу**:
   - Відредагуйте `popup.html` та `popup.js` для зміни інтерфейсу користувача
   - Стилі знаходяться всередині `popup.html` у тегу `<style>`

3. **Зміна логіки роботи з YouTube**:
   - Модифікуйте `content.js` для зміни способу виявлення та обробки субтитрів

4. **Зміна обробки аудіо**:
   - Налаштуйте `background.js` для зміни взаємодії з сервером
   - Змініть `server.py` для додавання нових можливостей синтезу мовлення

5. **Тестування змін**:
   - Після редагування файлів розширення, оновіть розширення на сторінці `chrome://extensions/` (натисніть іконку оновлення)
   - Для змін у сервері, перезапустіть сервер

## Поширені проблеми

### Розширення показує "Сервер не запущено"

**Рішення**:
- Переконайтеся, що Python-сервер запущено
- Перевірте, чи порт 3000 не зайнятий іншим додатком
- Перезапустіть сервер і браузер

### Субтитри не озвучуються

**Рішення**:
- Переконайтеся, що субтитри українською мовою увімкнені на відео
- Перевірте, чи озвучування увімкнено в налаштуваннях розширення
- Відкрийте консоль розробника в Chrome (F12) і перевірте наявність помилок

### Проблеми зі встановленням залежностей Python

**Рішення**:
- Оновіть pip: `python -m pip install --upgrade pip`
- Встановіть кожну залежність окремо
- Переконайтеся, що використовуєте Python 3.8 або новіше

### Віртуальне середовище не активується

**Рішення**:
- Для Windows: переконайтеся, що виконання скриптів дозволено. Запустіть PowerShell від імені адміністратора і виконайте: `Set-ExecutionPolicy RemoteSigned`
- Для macOS/Linux: переконайтеся, що шлях до venv правильний

### Помилка ModuleNotFoundError

**Рішення**:
- Переконайтеся, що віртуальне середовище активоване (`(venv)` на початку рядка)
- Перевстановіть залежності: `pip install -r edge-tts-server/requirements.txt`

## Ліцензія

[MIT License](LICENSE)

---

Створено з ❤️ для української спільноти.
